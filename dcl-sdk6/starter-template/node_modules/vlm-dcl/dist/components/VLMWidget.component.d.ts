export declare namespace VLMWidget {
    const configs: {
        [uuid: string]: VLMWidget.DCLConfig;
    };
    class DCLConfig {
        sk?: string;
        id: string;
        type?: ControlType;
        value?: string | number | boolean;
        user?: {
            connectedWallet: string;
            displayName: string;
        };
        order?: number;
        init?: (config: DCLConfig) => void;
        delete?: (config: DCLConfig) => void;
        update: (config: DCLConfig) => void;
        constructor(config: DCLConfig);
    }
    class VLMConfig extends DCLConfig {
    }
    enum ControlType {
        NONE = 0,
        TOGGLE = 1,
        TEXT = 2,
        SELECTOR = 3,
        DATETIME = 4,
        TRIGGER = 5,
        SLIDER = 6
    }
}
//# sourceMappingURL=VLMWidget.component.d.ts.map