/// <reference types="dcl" />
/// <reference types="env" />
import { PositionType } from "@decentraland/RestrictedActions";
import { VLMBase } from "./VLMBaseConfig.component";
export declare namespace VLMClickEvent {
    enum Actions {
        NONE = 0,
        EXTERNAL = 1,
        TRACKING_ONLY = 2,
        SOUND = 3,
        STREAM = 4,
        MOVE = 5,
        TELEPORT = 6
    }
    class DCLConfig {
        type: Actions;
        showFeedback: boolean;
        hoverText: string;
        externalLink?: string;
        sound?: string;
        moveTo?: {
            cameraTarget: PositionType;
            position: PositionType;
            setCameraTarget: boolean;
        };
        teleportTo?: string;
        hasTracking?: boolean;
        trackingId?: string;
        synced?: boolean;
        pointerDownEvent?: OnPointerDown;
        constructor(config: DCLConfig, clickable: VLMBase.Instance & IsClickable);
        trackClickEvent: CallableFunction;
    }
    interface IsClickable {
        clickEvent?: VLMClickEvent.DCLConfig;
    }
}
//# sourceMappingURL=VLMClickEvent.component.d.ts.map